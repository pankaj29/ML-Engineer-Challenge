# Extra dependencies needed only for training / dataset preparation, not for
# serving. Keeping them out of the serving image saves ~1 GB.
-r requirements.txt

# torch >= 2.6 imports onnxscript from inside torch.onnx.export. It is not a
# declared dependency of torch itself, so exporting fails with
# ModuleNotFoundError without it.
onnxscript~=0.1.0
timm~=1.0.11                    # pretrained classification backbones
ultralytics~=8.3.0              # YOLO object detection
tqdm~=4.67.0
requests~=2.32.0
matplotlib~=3.9.0
pandas~=2.2.0
scikit-learn~=1.5.0
tensorboard~=2.18.0

# Experiment tracking. Optional: --track none (the default) never imports it,
# and --track auto falls back to no tracking when it is absent.
mlflow~=3.1
